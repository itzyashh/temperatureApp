import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
    tempText:{
        fontSize:100,
        color:'white',
    },
    unit:{
        fontSize:20,
        color:'white',
    },
    root:{
        flexDirection:'row',
        alignItems:'baseline',
    }
})